---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by heyqule.
--- DateTime: 12/27/2025 12:52 AM
---

for index, data in pairs(storage.erm_unit_groups) do
    if type(data.attack_force) == 'string' then
        if game.forces[data.attack_force] then
            storage.erm_unit_groups[index].attack_force = game.forces[data.attack_force]
        else
            storage.erm_unit_groups[index].attack_force = game.forces['player']
        end
    end
end

if not settings.startup['enemyracemanager-enable-engineer-army'].value then
    storage.army_registered_deployers['army-depot'] = nil
    for idx, data in pairs(storage.army_windows_tab_player_data) do
        if data.deployer_type_filters['army-depot'] then
            storage.army_windows_tab_player_data[idx].deployer_type_filters['army-depot'] = nil
        end
    end
end