These tests are designed to run with default settings.  Changing certain settings may cause the test to fail.

e.g
- Settings which prevent other races to spawn
- Settings which change unit properties

